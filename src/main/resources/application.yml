spring:
  application:
    name: baedal-support-agent
  ai:
    openai:
      base-url: http://localhost:11434
      api-key: ollama
      chat:
        options:
          model: qwen2.5
          temperature: 0.3

logging:
  level:
    org.springframework.ai: DEBUG
